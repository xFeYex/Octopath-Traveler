# 文档文件定位

## Framework

定义：提供全局工具和基类

### 单例-全局唯一（Singleton）

**InputSystemController**

> 全局输入，用户怎么操控角色，由它定义 

**GameModeManager**

> 全局游戏模式管理，游戏模式切换需要通过它来改

PartyManager

> 全局团队管理，用于团队的跟随，战斗等内容、

InventoryManager

> 全局物品管理，用户存放用户身上的物品道具信息



### 事件-观察者（Event）

IEvent（发布者）

IEventReceiver（订阅者）

EventBus（事件总线）-> subscribe + unSubscribe + publish

1. 必须继承 **IEvent** 才能发布事件
2. 必须继续 **IEventReceiver** 才能订阅事件
3. 保证事件安全

## Game

定义：游戏核心业务逻辑

### camera

### character

### Game Mode

### input

### interaction

## Player

定义：玩家角色具体实现

## Path Action

定义：存放游戏指令相关的代码

完成了 Action Base（基类）

基础子类

1. InquireAction
2. RecruitAction



## Party

定义：存放跟团队相关的代码

fieldFollower -> 团队跟随代码

## UI

定义：绑定界面UI到代码中

## Utils

定义：全局类型定义和配置

Enum（枚举）

GlobalUsing（定义全局的应用）

# UI

## 持久化

1. Player -> 切换场景不会影响用户信息的持久化
2. Game Mode Manager -> 可以管理摄像头的切换 / 输入系统的控制
3. Camera -> 视角的切换（跟随 / 战斗 / 2D）

## 场景

# 全局管理类

1. UIManager -> 统一管理所有指令操作面板，创建新面板需到此注册

# 基类

1. IEvent

2. IEventReceiver

3. Singleton

4. InteractionBase

5. ActionBase

6. CharacterDefinitionSO

7. PanelController -> 所有指令操作面板的基类（打听、偷窃...）

8. ItemDefinitionSO -> 所有物品基础信息 基类

   

# 事件

## **发布者**

1. GameModeChangedEvent
2. InteractionEvents -> InteractionEvents(所有交互类的事件统一管理)
3. InteractionMenuRequsetEvent -> InteractionEvents(所有交互类的事件统一管理)
4. PanelRequestEvent -> PanelEvents(所有指令操作面板类的事情统一管理)

## **订阅者**

CameraController -> GameModeChangedEvent

InputSystemController -> GameModeChangedEvent

InteractionUIController -> InteractionEvents

InteractionUIController -> InteractionMenuRequsetEvent

UIManager -> PanelRequestEvent

# 设计模式

我只是基于在unity项目中用到的，以及我能扩展到的，做一个总结和复盘

## 三原则

**成本**

1. 理解成本
   - 为什么要养成写法的规范，降低理解成本就是其一
2. 调式成本
3. 阅读成本
   - 为什么要养成写法的规范，降低阅读成本就是其二
4. 沟通成本
   - 让我想到了DDD的统一语言，在一切一切的开始前，就要统一语言，不然很多沟通就是无效的

> 设计模式是为了降低“未来修改成本”，而不是提高“当前实现优雅度”

**理解**

1. 本质上是对**复杂问题**提供一套解决方案，设计模式是对原则的上层体现
2. 要可维护、有设计、能解耦
3. **软件最大的成本不是开发，而是修改**
4. 我们追求设计，不是为了设计而设计，一切要恰到好处，有些是为了即时，有些是为了未来
5. 极端些，可以这么说设计，它就是战未来的，但如果连未来都没有，那就根本不需要了
6. 组件是大于继承的，它更灵活
7. 稳定才是最值钱的，这才是核心代码

**原则**

1. **单一职责原则（srp）**
   - 一个模块只存在一个“变化原因”，而不是只做一件事情。我们要去降低能引起自身变换的因素，才是单一职责
   - Player类应该只专注于本身，具体怎么行动要交给其它类完成
   - 工欲善其事必先利其器。人之所以能强大是因为能善假于物，利用外物，我不管这个物怎么样，我能用即可。
2. **开闭原则（ocp）**
   - 一个已稳定的老代码，我们要增加新功能，不是去修改，而是基于老的去扩展代码。（对扩展支撑，对修改封闭）
   - 用扩展解决变化，而不是修改旧代码
3. **依赖倒置原则（dip）**
   - 高层依赖抽象，而不是依赖具体实现
   - 高层建筑不应依赖具体的底层建筑，而应该去依赖抽象的协议，这样才能到达更换。（就像主板一样，只要你能满足我的协议，上面的组件你可以随意插入）

## 1.命令模式

> 行为对象化（Behavior as Object）

**定义**（command）

1. 命令模式本质上是将**行为**封装为**对象**，从而让行为本身可以被**统一管理**。
2. 因为行为被对象化，所以它可以**像数据一样**被记录、撤销、排队、回放、同步等。

**游戏**

1. 像回合制，我可以将（攻击/防御/药水等）封装成一个命令，在队列中一个个执行，就可以完成诸如（撤回/执行间的特效/日志等）进阶的操作
2. 命令对象可以**保存**行为历史，因此能够实现回放、撤销等系统。

## 2.观察者模式

> 事件驱动解耦（Event Decoupling）

**定义**（Observer）

    1. 观察者模式定义了一种一对多的依赖关系，让多个观察者对象同时监听某一个主题对象。这个主题对象在状态发生变化时，会通知所有观察者对象，使它们能够自动更新自己。
    1. 唯一的缺点是调用链不可见，所以在管理上要严格
    1. 观察者模式本质是“事件驱动解耦”

**游戏**

1. 我们可以通过观察者模式进行解耦，如：我们击败一名敌人，会产生（1.击败特效；2.掉落物；3.玩家自身的增长；4.UI上的结算等）我们不可能把它们全部写下来然后去执行（写的位置会超出单一职责）。所以我们就可以通过订阅的形式，当击败的时候，发布订阅，完成解耦。

## 3.策略模式

**定义**

1. 目标 + 策略（是无序的）。是用户去调控的
2. 本质上是可替换的算法模式，基于同一个目标下的不同算法的切换。
3. 策略模式 = 把“算法选择权”从 if-else 中抽出来，让调用者决定使用哪个算法

```java
if(type == A) {}
else if(type == B) {}

变化为

strategy.Execute();
```

**游戏**

## 4.状态模式

**定义**

1. 状态 + 行为（是有序的）。是由系统去控制的
2. 本质是解决：一个对象有多种状态，不同的状态下，有不同的行为。
3. **状态模式的核心不是“状态很多”，而是“状态之间有转移规则”**
4. 状态模式 = “对象行为由当前状态决定，并且状态之间存在自动流转逻辑”
5. 状态模式应有（**状态图、转移条件、生命周期**），而策略模式没有

**游戏**

1. 有限状态机，无论是回合制游戏的进度（准备阶段、行动阶段、结算阶段），还是玩家的动画（站立、跑动、蹲下等）状态很多，我们可能去一个个连线完成，应该是通过代码的形式去完成，所以我们可以通过状态机的形式去完成

## 5.单例模式

**定义**

1. 具有唯一性的，不可被多次创建的实例
2. 单例 = 全局唯一访问点

**游戏**

​    1.全局UI管理、全局战斗管理等

# 写法习惯

本质为：可读性 + 一致性 + 降低维护成本

1. 全部为驼峰写法 （如: playerManager / SystemConfig）
   - 文件 + 变量名 + 函数 开头不大写
   - 类 + 结构体 + 枚举等 开头大写
   - 私有变量要加前缀 _ (如：_systemMode)
2. 命名格式为 动词 + 名词（行为 + 结果，如：createPlayer）

# 一些汇总

1. UI（UI制作，绑定到代码类中） - 核心逻辑（通过UI类与逻辑完成核心代码的编写实现） - 数据存储（最后完成数据的记录与读取）

# 模块完成汇报

## 第一部分 - base

1. 完成了基础的架构，单例模式、观察者模式的基础框架，可以被继承后使用
2. 定义了全局的游戏模式切换、摄像头的管理控制、全局的输入控制。
3. 分文件去管理代码，分了目前分了三部分Game、framework、player三部分。

## 第二部分 - inquire

1. UI绑定代码，代码控制UI完成效果实现
2. Unity整体的编写逻辑是先完成UI的绘制，代码绑定UI，然后用代码去控制UI。
3. 并不是所有的代码都需要添加MonoBehaviour，纯逻辑的代码并不一定要挂载。
4. 第二部分依旧是在第一个部分的基础上继续架构了，定义了指令、UI的基类为后面的扩展奠定了基础

## 第三部分 - recruit

1. 更多的还是基础上的架构继续写
   - 角色的详细数据啊（hp、bp，atk、def等）
   - 角色升级的曲线啊
   - 角色的运行数据 和 基础数据 和 装备属性的区分
   - 数据的加减乘除接口等底层功能的制作 
2. 有了前期好的底层架构，功能完成的是比较快的，还是一些简单的操作，一些基础的逻辑吧
3. 熟练一下工具后必须要自己去做一个简单的项目，将理论到实践，才是必要的

## 第四部分

## 第五部分

## 第六部分

## 第七部分

## 第八部分

# 当前代码与文档阶段性汇总分析

> 更新时间：2026-05-27  
> 范围：`Assets/Scripts` 当前代码结构 + 本文档已有架构/设计模式笔记

## 1. 当前项目整体架构定位

当前项目已经从“零散功能脚本”进入了“基础玩法系统成型”的阶段。代码整体采用的是 Unity 中比较典型的：

1. **MonoBehaviour 组件式架构**
2. **Manager 单例管理核心系统**
3. **EventBus 事件总线做模块解耦**
4. **ScriptableObject 数据驱动角色、物品、成长、装备**
5. **ActionBase + PanelController 组织 NPC 指令和 UI 面板**

目前项目中已经形成了几个比较清晰的模块：

| 模块 | 目录 | 当前职责 |
| --- | --- | --- |
| Framework | `Assets/Scripts/Framework` | 基础设施，提供单例、事件接口、事件总线 |
| Game | `Assets/Scripts/Game` | 游戏核心系统，包括模式、输入、相机、交互、队伍、背包、角色数据 |
| Player | `Assets/Scripts/Player` | 玩家移动、输入响应、触发 NPC 交互 |
| Path Action | `Assets/Scripts/Path Action` | NPC 可执行指令，如打听、招募、偷取、商店 |
| UI | `Assets/Scripts/UI` | 所有界面控制器，包括交互菜单、指令面板、背包、装备 |
| Utils | `Assets/Scripts/Utils` | 全局枚举、全局 using |

从架构上看，目前核心方向是正确的：**数据、系统、交互、UI 已经开始拆开**，并且每个模块都有比较明确的责任。

## 2. 文档内容与当前代码的对应关系

文档前半部分已经总结了当前项目的主要基础设施，例如：

1. `Singleton`：全局唯一管理器。
2. `IEvent / IEventReceiver / EventBus`：事件系统。
3. `ActionBase`：NPC 指令行为基类。
4. `InteractionBase`：NPC 交互入口。
5. `PanelController`：UI 指令面板基类。
6. `CharacterDefinitionSO / ItemDefinitionSO`：角色和物品数据基类。

这些内容和代码现状基本一致，而且是当前项目架构中最重要的几根主线。

但文档中有一些内容已经落后于当前代码进度：

1. `Path Action` 文档中只写到了 `InquireAction`、`RecruitAction`，当前代码已经扩展到 `StealAction` 和 `ShopAction`。
2. `Game` 模块下面的 `camera / character / Game Mode / input / interaction` 还只是标题，当前代码已经有较完整的 `Inventory`、`Party`、`Equipment` 相关内容。
3. `UIManager` 的描述中写了“创建新面板需到此注册”，但当前实际实现是通过扫描子节点中的 `PanelController`，再按 `PanelActionType` 建立字典，手动注册成本已经降低。
4. 设计模式笔记中已经讲了命令、观察者、策略、状态、单例，但还可以进一步补充“当前代码中具体哪里用到了这些模式”。

## 3. 当前核心运行链路

目前最完整的一条流程是 **NPC 交互链路**：

1. 玩家身上的 `PlayerInteraction` 通过 Trigger 检测到 NPC。
2. NPC 身上的 `InteractionBase` 缓存自身挂载的所有 `ActionBase`。
3. `InteractionBase` 根据队伍成员职业判断哪些指令可以显示。
4. `InteractionBase` 发布 `InteractionChangedEvent`。
5. `InteractionUIController` 订阅事件，显示 NPC 头顶可交互图标。
6. 玩家按确认键后，`InteractionBase` 发布 `InteractionMenuRequestEvent`。
7. `InteractionUIController` 打开二级指令菜单。
8. 玩家选择具体指令后，调用对应 `ActionBase.TriggerAction`。
9. 具体 Action 发布 `PanelRequestEvent`。
10. `UIManager` 根据 Action 类型找到对应 `PanelController` 并打开面板。
11. 面板中执行具体逻辑，例如打听、招募、偷取、买卖。

这条链路体现了当前项目最核心的架构思想：

> NPC 本身只负责提供可交互行为，UI 只负责表现和操作，具体指令由 Action 负责承载。

这是一个比较适合 RPG 项目的扩展方式。以后新增“挑战”“引导”“诱惑”“雇佣”等路径行动时，不需要重写整套交互系统，只需要新增 Action 和对应 Panel。

## 4. 当前使用到的设计模式

### 4.1 单例模式 Singleton

当前使用位置：

1. `GameModeManager`
2. `InputSystemController`
3. `InventoryManager`
4. `PartyManager`

优点：

1. 使用简单，任何地方都可以通过 `Instance` 访问核心系统。
2. 适合当前项目阶段快速搭建全局系统。
3. 对输入、游戏模式、背包、队伍这类全局唯一系统比较直观。

缺点：

1. 隐藏依赖较多，类之间的真实依赖关系不容易从构造或字段中看出来。
2. 初始化顺序需要小心，例如某些数据初始化时依赖 `InventoryManager.Instance`。
3. 后续做多场景、存档读档、测试时会变得更难管理。

### 4.2 观察者模式 Observer / EventBus

当前使用位置：

1. `GameModeChangedEvent`
2. `InteractionChangedEvent`
3. `InteractionMenuRequestEvent`
4. `PanelRequestEvent`

当前事件订阅者：

1. `CameraController` 监听游戏模式变化，切换相机。
2. `InputSystemController` 监听游戏模式变化，切换输入 ActionMap。
3. `InteractionUIController` 监听交互事件，刷新头顶图标和交互菜单。
4. `UIManager` 监听面板请求事件，打开对应指令面板。

优点：

1. 发布者不需要知道接收者是谁。
2. 输入、相机、UI、交互系统之间耦合降低。
3. 很适合处理“某件事发生后，多个系统都要响应”的场景。

缺点：

1. 调用链不可见，排查问题时需要查事件发布和订阅关系。
2. 静态 EventBus 需要注意生命周期和订阅释放。
3. 当前 `Publish` 中遍历列表时会移除空对象，有可能跳过后一个 receiver，后续建议优化为倒序遍历或先复制列表。

### 4.3 命令模式 / 策略模式

当前使用位置：

1. `ActionBase`
2. `InquireAction`
3. `RecruitAction`
4. `StealAction`
5. `ShopAction`

`ActionBase` 把 NPC 行为抽象为统一接口：

1. `CanShow`：是否显示。
2. `CanExecute`：是否能执行。
3. `TriggerAction`：触发行为。
4. `Execute`：执行实际效果。

这让 NPC 的行为可以像组件一样自由组合。比如一个 NPC 可以同时挂 `InquireAction` 和 `ShopAction`，另一个 NPC 可以挂 `RecruitAction`。

这个设计目前非常适合项目现阶段，因为它满足了开闭原则：

> 新增行为时，通过新增 Action 子类扩展，而不是修改 `InteractionBase`。

### 4.4 状态模式雏形

当前使用位置：

1. `GameMode`
2. `GameModeManager`
3. `InputSystemController`
4. `CameraController`
5. `UIManager`

当前 `GameMode` 包含：

1. `Explore`
2. `Battle`
3. `InteractionMenu`
4. `Pause`

目前还不是完整的状态模式，更像是“枚举状态 + 管理器切换”。但它已经承担了状态机的核心职责：

1. 不同模式下启用不同输入。
2. 不同模式下显示不同 UI。
3. 不同模式下切换不同相机。

后续如果战斗流程变复杂，可以进一步升级为真正的状态机，例如：

1. `ExploreState`
2. `BattlePrepareState`
3. `BattleCommandState`
4. `BattleActionState`
5. `BattleResultState`

### 4.5 数据驱动 ScriptableObject

当前使用位置：

1. `CharacterDefinitionSO`
2. `AllyDefinitionSO`
3. `EnemyDefinitionSO`
4. `GlobalGrowthConfigSO`
5. `ItemDefinitionSO`
6. `ConsumableItemSO`
7. `EquipmentItemSO`
8. `ItemIconSetSO`

优点：

1. 角色、物品、装备、成长曲线可以直接在 Unity Inspector 中配置。
2. 运行时数据和静态配置开始分离。
3. 很适合 RPG 项目后续做数值调整。

当前已经有比较好的区分：

1. `CharacterDefinitionSO`：角色静态定义。
2. `CharacterRuntimeData`：角色运行时数据。
3. `EquipmentItemSO`：装备静态定义。
4. `InventoryItem`：背包中的物品和数量。

## 5. 当前架构优点

### 5.1 模块职责已经比较清晰

`Framework`、`Game`、`Player`、`Path Action`、`UI`、`Utils` 的目录划分比较清楚。虽然还没有 Assembly Definition 或 namespace 完整隔离，但已经具备了模块化意识。

### 5.2 NPC 指令扩展性较好

`ActionBase` + `InteractionBase` 的设计是当前代码中最值得保留和继续强化的部分。NPC 行为以组件形式挂载，天然符合 Unity 的组合思想。

### 5.3 事件系统降低了部分耦合

例如游戏模式变化后，输入系统和相机系统都能通过事件响应，而不是由 `GameModeManager` 直接持有它们的引用。这比管理器互相调用更干净。

### 5.4 UI 面板已有统一基类

`PanelController` 统一了：

1. 当前 Action。
2. 默认按钮焦点。
3. 关闭面板。
4. 确认逻辑。
5. 取消输入处理。

这对后续继续添加菜单和子面板很重要。

### 5.5 角色、背包、装备系统已经开始形成 RPG 数据模型

当前代码已经开始区分：

1. 角色基础数据。
2. 角色运行时数据。
3. 装备属性加成。
4. 背包数量。
5. 已装备数量。
6. 装备候选列表。

这说明项目已经不只是 UI Demo，而是开始有真正的 RPG 系统骨架。

## 6. 当前架构缺点与风险

### 6.1 单例访问过多，隐藏依赖增加

当前很多类直接访问：

1. `InventoryManager.Instance`
2. `PartyManager.Instance`
3. `GameModeManager.Instance`
4. `InputSystemController.Instance`

短期写起来快，但长期会产生几个问题：

1. 类之间依赖关系不明显。
2. 初始化顺序容易出问题。
3. 单元测试困难。
4. 切换场景或重载数据时容易出现旧状态残留。

建议后续把“稳定全局系统”继续保留单例，但业务规则尽量收敛到 Service 或数据对象中。

### 6.2 UI 与业务逻辑仍然耦合

例如：

1. `ShopPanelController` 直接调用 `ShopAction.TryExecuteTransaction`。
2. `ItemPanelController` 直接读取 `InventoryManager` 和 `PartyManager`。
3. `EquipmentPanelController` 同时负责 UI 焦点、列表生成、装备变更、属性预览。

这些在当前阶段可以接受，但后续功能增加后，UI Controller 会越来越重。

建议逐步把复杂规则放入 Service，例如：

1. `ShopService`
2. `InventoryService`
3. `EquipmentService`
4. `PathActionService`

目前已有 `EquipmentService`，这是一个好的方向。

### 6.3 EventBus 需要继续强化

当前 EventBus 已经能工作，但后续建议处理：

1. 发布事件时避免边遍历边删除导致跳过接收者。
2. 增加清理接口，方便场景卸载或测试。
3. Debug 模式下可以记录事件发布日志，降低排查成本。
4. 事件命名保持统一，例如 `Request`、`Changed`、`Completed`。

### 6.4 数据封装还不够严格

当前很多字段是 public，例如：

1. `InventoryManager.CurrentInventory`
2. `InventoryManager.Currency`
3. `CharacterRuntimeData.Level`
4. `CharacterRuntimeData.CurrentHP`
5. `ActionBase.CommandInfo`

这方便 Inspector 和调试，但也意味着外部可以随意修改数据。

后续建议：

1. Inspector 需要配置的字段用 `[SerializeField] private`。
2. 对外只暴露只读属性。
3. 修改数据通过方法完成，例如 `AddItem`、`RemoveItem`、`SetEquippedItem`。

### 6.5 部分代码存在明显可修复问题

当前静态阅读中发现几个需要注意的点：

1. `CharacterRuntimeData.GetBaseStats` 的空判断逻辑疑似写反，`Definition is null ? Definition.BaseStats : StatBlock.zero` 会在空时访问空对象。
2. `CharacterRuntimeData.Level` 没有在构造时从 `Definition.BaseLevel` 初始化。
3. 初始装备在 HP/SP 初始化之后应用，可能导致角色初始 HP/SP 不是装备后的最大值。
4. `GameModeManager.AppleMode` 应该是 `ApplyMode`，命名可修正。
5. `PlayerController.Movemnet` 应该是 `Movement`。
6. `InteractionUIController` 中 `_targer` 应该是 `_target`。
7. `ActionMenuBotton` 应该是 `ActionMenuButton`。
8. 部分 UI 文案有误，例如商店出售弹窗仍写“确认购买”。

这些不影响架构方向，但会影响后续维护质量，建议阶段性统一修一次。

## 7. 当前阶段项目总结

当前项目已经完成了一个 RPG 原型中非常重要的基础闭环：

1. 玩家移动。
2. NPC 交互检测。
3. 可用指令筛选。
4. 交互菜单显示。
5. 指令面板打开。
6. 打听、招募、偷取、商店等路径行动。
7. 队伍成员管理。
8. 背包物品管理。
9. 装备候选、装备切换、属性预览。
10. 输入模式和游戏模式切换。

从学习和工程实践角度看，这一阶段最重要的收获是：

> 已经开始从“功能能跑”进入“系统之间如何组织”的阶段。

也就是说，现在的问题不再只是“怎么写一个功能”，而是：

1. 新功能应该放在哪个模块？
2. 谁负责数据？
3. 谁负责表现？
4. 谁负责规则？
5. 谁通过事件通知别人？
6. 谁可以直接调用谁？

这是项目开始真正进入架构思考的标志。

## 8. 下一阶段建议

### 8.1 先修正基础 bug 和命名问题

优先处理：

1. `CharacterRuntimeData.GetBaseStats` 空判断。
2. `Level` 初始化。
3. 初始装备后 HP/SP 刷新。
4. 明显拼写错误。
5. 商店文案错误。

这些属于低风险高收益修改。

### 8.2 强化 GameMode 状态流转

建议为 `GameModeManager` 增加明确的模式切换规则，例如：

| 当前模式 | 允许切换到 |
| --- | --- |
| Explore | Pause / InteractionMenu / Battle |
| Pause | Explore |
| InteractionMenu | Explore |
| Battle | BattleResult / Explore |

这样可以避免任何系统随意切换模式。

### 8.3 继续拆分 UI Controller 中的业务逻辑

当前 `EquipmentService` 是一个好方向。后续可以继续增加：

1. `ShopService`：处理购买、出售、价格、货币检查。
2. `InventoryService`：处理背包增删、堆叠、可用数量。
3. `PathActionService`：处理路径行动的通用规则。

目标是让 UI Controller 更像“表现层”，而不是同时承担规则层。

### 8.4 建立统一代码规范

建议逐步统一：

1. 类名、文件名拼写。
2. private 字段 `_camelCase`。
3. public 属性 `PascalCase`。
4. 方法名使用动词开头。
5. 事件命名使用 `XXXEvent`。
6. 请求类事件使用 `XXXRequestEvent`。
7. 状态变化事件使用 `XXXChangedEvent`。

### 8.5 为后续战斗系统提前准备

目前已有：

1. `CharacterRuntimeData`
2. `StatBlock`
3. `GameMode.Battle`
4. `CameraView.Battle`
5. `ActionMap.Battle`

这些都说明战斗系统已经有入口，但还没有真正实现。后续战斗系统可以基于当前数据模型继续扩展：

1. 战斗状态机。
2. 战斗指令 Command。
3. 技能/道具目标选择。
4. 回合队列。
5. 伤害公式 Service。
6. 战斗结算事件。

## 9. 阶段性评价

当前架构的优点是：**方向正确、模块意识清晰、可扩展点已经出现**。尤其是 `ActionBase`、`PanelController`、`EventBus`、`ScriptableObject` 数据驱动这几部分，已经能支撑继续扩展 RPG 的核心玩法。

当前架构的主要问题是：**便利性写法开始积累技术债**。单例访问、UI 直接操作业务、public 数据字段、拼写命名不统一，这些现在还不严重，但如果继续扩展战斗、任务、存档、场景切换，就会逐渐放大。

所以当前阶段最合适的目标不是大重构，而是：

> 保留现有架构主线，修正明显问题，逐步把复杂规则从 UI 和 Manager 中抽出来。

这样既不会打断开发节奏，也能让项目架构继续变稳。

# 其它
